return {
  {
    "LazyVim/LazyVim",
    opts = {
      -- Just use the "default" colorscheme that is actaully a colorscheme from lazy.
      -- So there's no need to download it.
      colorscheme = "default",
    },
  },
}
